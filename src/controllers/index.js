const rootController = require('./rootController');
const userController = require('./userController-alex');
const excelController = require('./excelController');

module.exports = {
  rootController,
  userController,
  excelController,
};
